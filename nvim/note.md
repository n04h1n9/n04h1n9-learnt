顺带一提，有几个非常有用的 Neovim 默认快捷键我可以在这里讲一下。第一个是 %，可以用于在前后的括号（包括花括号、圆括号、方括号等）之间快速跳转；第二个是 * 和 #，分别用于向下和向上查找当前光标所在的单词并跳转到对应位置；第三个是 Ctrl-O 和 Ctrl-I，分别用来跳转到前一个位置和跳转到下一个位置（jump list），比如这里的 Ctrl-O 常用于“跳转到函数定义”之后再跳回来；第四个是 gd/gD/gi/gr，分别用于跳转到定义/跳转到声明/跳转到实现/跳转到引用；第五个是单个大写字母 K，用于查看当前光标所在位置的帮助信息（比如类型信息、函数签名等）
